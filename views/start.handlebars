     <div>
         <div class="row">
             <div style="text-align: center;">
                <h2>Let's Start</h2>
                <h3>Add our favorites to your library</h3>
            </div>
        </div>

        
            <div class="d-flex justify-content-center" style="margin:5% 10% 5% 15%;">
                <div class="row row-cols-6 row-cols-sm-2 row-cols-md-4 row-cols-lg-6">


                    {{!-- THIS IS THE SECTION WE NEED TO MAKE DYNAMIC --}}
                    
                     {{!-- END --}}
 

                    {{#each books as |book| }}
                    <div>
                        <div class="add-book-card">
                        <div style="position:relative;">
                                <button type="submit" class="add-book-button">
                                <p class="add-book-icon">+</p>
                                <img class="book-cover" src="/images/{{book.image}}" alt="{{book.title}} image" width="auto" height="150px"/>
                            </button>
                        </div>
                     </div>
                    </div>
                    {{/each}}
                    
            </div>
        </div>
    </div>